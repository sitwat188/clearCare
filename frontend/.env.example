# Backend API URL
VITE_API_BASE_URL=http://localhost:3000/api/v1

# Use mock data (set to 'false' to use real backend)
VITE_USE_MOCK_DATA=false

# Fasten Connect – use YOUR project's public ID from https://connect.fastenhealth.com
# If you use another project's ID, the Stitch UI/branding and success messages will show that project, not yours.
# Get your public ID (and backend private key) from your Fasten Connect organization / API keys.
VITE_FASTEN_PUBLIC_ID=
